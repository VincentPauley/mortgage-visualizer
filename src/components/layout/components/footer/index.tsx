import { Layout } from 'antd';
import './footer.css';

const { Footer } = Layout;

const AppFooter = () => {
  return <Footer className="AppFooter">Application Footer</Footer>;
};

export default AppFooter;
